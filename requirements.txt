# Production dependencies for Market Research Intelligence Assistant
# Install: pip install -r requirements.txt

# Core FastAPI stack
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
pydantic-settings>=2.5.0

# Database
sqlalchemy>=2.0.0

# Authentication (Phase 4)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTP & Web Scraping (Phase 1)
httpx>=0.27.0
trafilatura>=1.12.0
lxml[html-clean]>=4.9.0

# LLM & LangChain (Phase 2)
langchain-core>=0.3.0
langchain-google-genai>=2.0.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0

# SSE Streaming (Phase 2.5)
sse-starlette>=2.1.0

# Templating (Phase 2)
jinja2>=3.1.0